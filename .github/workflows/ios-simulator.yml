name: iOS Simulator Build

on:
  workflow_call:
    inputs:
      build_id:
        required: true
        type: string
      node_version:
        required: false
        type: string
        default: '24.11'
      ruby_version:
        required: false
        type: string
        default: '3.3'
      pnpm_version:
        required: false
        type: string
        default: '10'
    secrets:
      APPNATIVELY_SECRETS:
        required: true

jobs:
  build-ios-simulator:
    runs-on: macos-latest
    env:
      NODE_OPTIONS: "--max-old-space-size=4096"

    steps:
      - name: Download from R2
        uses: appnatively/workflows/.github/actions/download-r2@master

      - name: Setup Scripts
        uses: appnatively/workflows/.github/actions/setup-scripts@master

      - name: Fetch App Configuration
        env:
          BUILD_ID: ${{ github.event.inputs.build_id || inputs.build_id }}
          SECRETS_JSON: ${{ secrets.APPNATIVELY_SECRETS }}
        run: |
          API_URL=$(echo "$SECRETS_JSON" | jq -r '.EXPO_PUBLIC_API_URL // empty')
          if [ -z "$API_URL" ]; then
            echo "❌ EXPO_PUBLIC_API_URL not found in secrets."
            exit 1
          fi
          echo "📡 Fetching config from ${API_URL}/mobile-app/builds/${BUILD_ID}/config"
          curl -s "${API_URL}/mobile-app/builds/${BUILD_ID}/config" > app_config.json
        working-directory: app


      - name: Setup App Info (Dynamic Package ID and App Name)
        run: ./scripts/setup-app-info.sh
        env:
          BUILD_ID: ${{ github.event.inputs.build_id || inputs.build_id }}
          API_URL: ${{ secrets.EXPO_PUBLIC_API_URL }}
        working-directory: app

      - name: Get iOS Project Name
        id: ios-name
        run: |
          if [ -f app/.ios_project_name ]; then
            echo "name=$(cat app/.ios_project_name)" >> $GITHUB_OUTPUT
          else
            echo "name=AppNativelyApp" >> $GITHUB_OUTPUT
          fi

      - name: Setup Mobile Environment
        uses: appnatively/workflows/.github/actions/setup-mobile@master
        with:
          ios: true
          node_version: ${{ inputs.node_version }}
          ruby_version: ${{ inputs.ruby_version }}
          pnpm_version: ${{ inputs.pnpm_version }}

      - name: Download App Assets & Inject Dynamic Environment Variables
        env:
          BUILD_ID: ${{ github.event.inputs.build_id || inputs.build_id }}
          SECRETS_JSON: ${{ secrets.APPNATIVELY_SECRETS }}
        run: |
          echo "⬇️ Downloading app assets from Google Drive..."
          ./scripts/setup-assets.sh

          echo "💉 Injecting dynamic environment variables into JS bundle..."
          for var in EXPO_PUBLIC_APP_ID EXPO_PUBLIC_API_URL; do
            VALUE=$(echo "$SECRETS_JSON" | jq -r ".$var // empty")
            if [ -n "$VALUE" ]; then
              echo "✅ Injecting $var"
              sed -i.bak "s|__PLACEHOLDER_${var}__|${VALUE}|g" main.jsbundle && rm main.jsbundle.bak
            fi
          done
        shell: bash
        working-directory: app

      - name: Build Simulator App
        run: |
          set -o pipefail
          xcodebuild build \
            -workspace ${{ steps.ios-name.outputs.name }}.xcworkspace \
            -scheme ${{ steps.ios-name.outputs.name }} \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO | xcpretty
        working-directory: app/ios

      - name: Zip Simulator App
        run: |
          cd build/Build/Products/Debug-iphonesimulator
          zip -r ../../../../${{ steps.ios-name.outputs.name }}-simulator.app.zip *.app
        working-directory: app/ios

      - name: Upload Build Log on Failure
        if: failure()
        run: |
          echo "### Build Error Trace 🚨" >> $GITHUB_STEP_SUMMARY
          tail -n 200 build.log >> $GITHUB_STEP_SUMMARY || true
        working-directory: app/ios

      - name: Setup Firebase
        env:
          BUILD_ID: ${{ github.event.inputs.build_id || inputs.build_id }}
          SECRETS_JSON: ${{ secrets.APPNATIVELY_SECRETS }}
        run: |
          # Setup Firebase (fetch and run from workflows repo)
          echo "🔥 Setting up Firebase..."
          ./scripts/setup-firebase.sh

          # 1. Find the .app bundle in DerivedData
          APP_PATH=$(find build/Build/Products/Debug-iphonesimulator -name "*.app" -type d -maxdepth 1)
          if [ -z "$APP_PATH" ]; then exit 1; fi
          
          cp main.jsbundle "$APP_PATH/"
          if [ -d "assets" ]; then cp -R assets "$APP_PATH/"; fi

          if [ -f "GoogleService-Info.plist" ]; then
            cp "GoogleService-Info.plist" "$APP_PATH/"
          fi

          zip -r ../../app-ios-simulator.zip "$APP_PATH"
        working-directory: app/ios

      - name: Generate Job Summary
        if: always()
        uses: appnatively/workflows/.github/actions/generate-summary@master
        with:
          title: '🤖 iOS Simulator Build Summary'
          metric_name: 'App Size'
          file_path: 'app-ios-simulator.zip'
          platform: 'iOS Simulator'
          build_id: ${{ inputs.build_id }}

      - name: Upload to Google Drive
        uses: appnatively/workflows/.github/actions/upload-google-drive@master
        with:
          source: app-ios-simulator.zip
          build_id: ${{ inputs.build_id }}
          appnatively_secrets: ${{ secrets.APPNATIVELY_SECRETS }}
